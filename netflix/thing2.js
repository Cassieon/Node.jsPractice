//file should export a function that prints out a list of
//names (that can be passed in) with 'ZZZ' added to the end of it

import listOfNames = require("./thing1");

//import listOfNames = require("./thing1");

//let names = [0, 1, 2, 3]

// names.forEach(name => {
//     console.log(name)

//const listOfNames = ['billy', 'joe', 'gruff'];


// function poppins () {
//     const listOfNames = ['billy', 'joe', 'gruff'];
//     let ending = (`${listOfNames}espialidocius`)

// }
// poppins();

let ending = '';
function eachName(poppins) {
    for (let index = 0; index < listOfNames.length; index++) {
        //ending += 'espialidocious'
    } 
    //poppins[currentIndex] = ending;
}